[pytest]
# Tests live alongside this file and run in-container (see conftest.py).
testpaths = .
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    memory: memory-usage / leak regression tests (may require pytest-memray)
